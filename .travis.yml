language: java
before_install:
  - wget https://github.com/sunyifan112358/pvis/releases/download/0.0.1/pvis-0.0.1.jar -p library
  - mvn validate
script: mvn test
after_success:
  - mvn clean test jacoco:report coveralls:report
jdk:
  - oraclejdk8